{
  "//": "Phase 10 frontmatter schema. Single edit surface for the validator (scripts/frontmatter_lint.py). The Phase-09 starter's two contested fields are now settled and are ERRORS: `intention` is coarse, `register` is derived (never stored), `constitutive` is required. What is still genuinely undecided stays a warning. See phases/refoundation/phase-10-schema/plan.md and the records in that folder.",

  "required": ["title", "force", "intention", "constitutive", "view", "provenance", "audience", "reader", "status"],

  "error_enums": {
    "view": ["synchronic", "diachronic"],
    "reader": ["H", "M", "H+M"],
    "force": ["orient", "explain", "describe", "instruct", "teach", "recommend", "mandate", "commit", "propose", "decide", "prove", "account"],
    "provenance_type": ["function", "project"],
    "intention": ["state", "formative", "suasive", "affective"],
    "constitutive": ["yes", "no"]
  },

  "audience_base_roles": ["user", "contributor", "decider"],

  "root_only_fields": ["axis", "dominant-community"],

  "derived_fields": {
    "register": "constitutive == yes ? govern : direction_of_fit(force) — derived, never stored. Storing it is an error.",
    "diataxis": "force x intention, where the register is know or do.",
    "direction-of-fit": "fixed by force — see the table below."
  },

  "direction_of_fit": {
    "know": ["orient", "explain", "describe", "prove", "account"],
    "do": ["instruct", "teach", "recommend", "mandate", "commit", "propose", "decide"]
  },

  "optional_fields": {
    "hash": "sha256:<64 lowercase hex> — the fixity seal of a frozen record (scripts/hash_seal.py mints it, scripts/hash_check.py verifies it).",
    "verb": "generative verb; stored only when it diverges from the tuple.",
    "id": "ULID (ADR-028); `slug`: the filename stem.",
    "concerns": "the activities a document governs, distinct from its provenance.",
    "distance": "recipient distance; `power`: the sanction the recipient holds.",
    "supersedes": "`superseded-by`, `retention`, `pin`, `order`, `phase`."
  },

  "hash_pattern": "^sha256:[0-9a-f]{64}$",

  "warn_enums": {
    "distance": ["initiated", "novice", "peer", "near", "far"],
    "power": ["none", "low", "holds"],
    "status": ["draft", "stable", "deprecated", "accepted", "done", "in-progress", "planned"]
  },

  "still_open": [
    "distance/power allowed values — the sets above are the current practice, not a ruling",
    "status vocabulary — current documents vs frozen records use different words",
    "deriving `constitutive` from force x power — deferred by decision (see register-derived-constitutive.md)",
    "the shape of `concerns`"
  ]
}
