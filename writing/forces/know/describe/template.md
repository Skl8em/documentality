---
title: "<The thing> reference"
force: describe
verb: specify
intention: state
view: synchronic
provenance: { type: function, id: <project-id> }
audience: [user, contributor]
reader: H+M
distance: far
power: none
status: draft
---

<!--
TEMPLATE — force `describe` (verb: specify). Stance: ./README.md
State the facts exhaustively, in a stable order. Organize by the subject.
Fail the moment it starts explaining WHY (that is `explain`, a separate file).
Delete these comments once filled in.
-->

## Scope

<What this reference covers, and — explicitly — what it does not.>

## <Facts, in a stable predictable order>

<State each fact plainly. Same shape for every entry so the reader navigates by
structure. Exemplify only where a fact is ambiguous.>

## Out of scope

<Mark the bounds: what a reader might expect here but must find elsewhere, with a
pointer to where.>
